package org.dubok.classfile.signature.tree;

import org.dubok.classfile.signature.SignatureTree;

public class TypeArguments extends SignatureTree {
    public TypeArguments(SignatureTree parent) {super(parent);}
}
