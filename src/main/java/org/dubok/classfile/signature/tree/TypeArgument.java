package org.dubok.classfile.signature.tree;

import org.dubok.classfile.signature.SignatureTree;

public class TypeArgument extends SignatureTree implements Element{
    public TypeArgument(SignatureTree parent) {super(parent);}
}
