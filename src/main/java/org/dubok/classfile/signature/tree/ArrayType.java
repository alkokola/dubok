package org.dubok.classfile.signature.tree;

import org.dubok.classfile.signature.SignatureTree;

public class ArrayType extends SignatureTree {
    public ArrayType(SignatureTree parent) {super(parent);}
}
