package org.dubok.classfile.signature.tree;

import org.dubok.classfile.signature.SignatureTree;

public class SuperclassSignature extends SignatureTree {
    public SuperclassSignature(SignatureTree parent) {super(parent);}
}
