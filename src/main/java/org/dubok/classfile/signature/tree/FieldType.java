package org.dubok.classfile.signature.tree;

import org.dubok.classfile.signature.SignatureTree;

public class FieldType extends SignatureTree {
    public FieldType(SignatureTree parent) {super(parent);}
}
