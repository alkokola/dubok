package org.dubok.classfile.signature.tree;

import org.dubok.classfile.signature.SignatureTree;

public class VoidDescriptor extends SignatureTree {
    public VoidDescriptor(SignatureTree parent) {super(parent);}
}
