package org.dubok.classfile.signature.tree;

import org.dubok.classfile.signature.SignatureTree;

public class TypeParameters extends SignatureTree {
    public TypeParameters(SignatureTree parent) {super(parent);}
}
