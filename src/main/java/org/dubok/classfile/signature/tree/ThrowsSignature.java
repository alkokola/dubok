package org.dubok.classfile.signature.tree;

import org.dubok.classfile.signature.SignatureTree;

public class ThrowsSignature extends SignatureTree {
    public ThrowsSignature(SignatureTree parent) {super(parent);}
}
