package org.dubok.classfile.signature.tree;

import org.dubok.classfile.signature.SignatureTree;

public class ParameterDescriptor extends SignatureTree implements Element {
    public ParameterDescriptor(SignatureTree parent) {super(parent);}
}
