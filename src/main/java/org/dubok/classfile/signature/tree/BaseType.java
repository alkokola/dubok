package org.dubok.classfile.signature.tree;

import org.dubok.classfile.signature.SignatureTree;

public class BaseType extends SignatureTree {
    public String type;
    public BaseType(SignatureTree parent) {super(parent);}
}
