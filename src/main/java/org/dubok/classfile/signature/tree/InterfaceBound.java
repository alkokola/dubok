package org.dubok.classfile.signature.tree;

import org.dubok.classfile.signature.SignatureTree;

public class InterfaceBound extends SignatureTree implements Element {
    public InterfaceBound(SignatureTree parent) {super(parent);}
}
