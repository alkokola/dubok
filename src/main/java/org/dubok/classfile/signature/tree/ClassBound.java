package org.dubok.classfile.signature.tree;

import org.dubok.classfile.signature.SignatureTree;

public class ClassBound extends SignatureTree implements Element{
    public ClassBound(SignatureTree parent) {super(parent);}
}
