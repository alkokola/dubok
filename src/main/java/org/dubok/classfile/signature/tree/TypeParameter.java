package org.dubok.classfile.signature.tree;

import org.dubok.classfile.signature.SignatureTree;

public class TypeParameter extends SignatureTree implements Element{
    public TypeParameter(SignatureTree parent) {super(parent);}
}
