package org.dubok.classfile.signature.tree;

import org.dubok.classfile.signature.SignatureTree;

public class JavaTypeSignature extends SignatureTree implements Element {
    public JavaTypeSignature(SignatureTree parent) { super(parent); }
}
