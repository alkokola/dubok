package org.dubok.classfile.signature.tree;

import org.dubok.classfile.signature.SignatureTree;

public class PackageSpecifier extends SignatureTree {
    public PackageSpecifier(SignatureTree parent) {super(parent);}
}
